# Core ML and DL
tensorflow>=2.9
numpy>=1.21
scikit-learn>=1.0

# Data manipulation and processing
pandas>=1.3

# Optional: Jupyter for notebooks (if you use them)
jupyterlab

# Requests for downloading battle logs
requests>=2.26

# tqdm for progress bars (optional but helpful)
tqdm

# matplotlib for plots (optional)
matplotlib